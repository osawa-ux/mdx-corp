---
status: accepted
date: 2026-08-20
decision-makers: 院長
published: true
---
# 0003: 応募フォームの所有アカウントと置き場所を確定する（ADR-0001 案C の実装前提を supersede）

## Context and Problem Statement

ADR-0001 は「オーナー権限の移管は Google 仕様上できない」ことを受けて**案C＝作り直し**を採用した。その案C の記述は置き場所を **`@mdx-inc.co.jp` の共有ドライブ**と書いていた。2026-08-19〜20 に実装しようとしたところ、その前提が 2 点とも成り立たないことが実測で判明した。

### 判明した事実（2026-08-19〜20 実測）

1. **`mdx-inc.co.jp` は横浜ホームクリニックのテナントとは別テナントである。**
   院長の super-admin アカウントで管理コンソール（`admin.google.com/ac/domains/manage`）を開いて実測。クリニック側テナント（組織名「横浜ホームクリニック」。顧客IDは本 repo が public のため記載しない＝ADR-0002 の方針）のドメイン一覧は `yokohama-home.jp`（プライマリ）/ `yokohama-home.jp.guest.google`（ゲスト）/ `yokohama-home.jp.test-google-a.com`（テストエイリアス）の 3 件のみで、**`mdx-inc.co.jp` は含まれない**。
   - ⚠ この判定に**至らなかった**測り方も記録する。「同一 DWD クライアントが両ドメインの subject で impersonate できるか」は**判別力がない**。mail-ops の `docs/dwd_setup.md` が「同じ Client ID + scope を 3 Workspace の管理コンソールで繰り返し登録する」運用を明記しており、別テナント 3 件でも同じ結果になるため。メールヘッダによる内外判定も、陽性対照（同一ドメイン内メール）が同じヘッダを持つため判別力なしと確定した。
2. **共有ドライブ内のフォームでは「ファイルのアップロード」設問を作れない。**
   UI では選択肢がグレーアウトし、Forms API は `Creation of file_upload question not supported` を返す。**切り分け**: 同一アカウント（`info@mdx-inc.co.jp`）の**マイドライブ**に作った検証用フォームでは同項目が**有効**だった。したがって原因はエディションでもテナントでもなく**置き場所**である。
   旧フォームには「履歴書・職務経歴書」（最大 5 件・10MB）のアップロード設問があるため、**共有ドライブと機能パリティは両立しない**。

### 院長決定（2026-08-20・2 回明示）

MDX株式会社は横浜ホームクリニックの MS法人であり、院長が株式を 100% 保有する実質的支配者である。**今後の MDX 系のフォーム・ドライブは `osawa@yokohama-home.jp` で運用し、`@yokohama-home.jp` 全体に共有する。** 委託関係に関する法務論点は顧問の西村先生に相談済みで決着している。

## Considered Options

- **案C-1: `@mdx-inc.co.jp` の共有ドライブ（ADR-0001 の当初案）** — 上記 2 により履歴書アップロード設問を置けない。採用しない。
- **案C-2: `info@mdx-inc.co.jp` のマイドライブ** — 履歴書設問は置ける。ただし院長決定により採用しない。
- **案C-3: `osawa@yokohama-home.jp` のマイドライブ（採用）** — 履歴書設問を置け、`yokohama-home.jp` 全体への共有で院内の運用に載る。**フォーム本体**の所有は無償個人アカウントではなく **Workspace アカウント**になる。
  ⚠ **ただし ADR-0001 が解こうとした問題は半分しか解けていない。** 下記 Decision Outcome の 回答先の決定により、**応募者の実データが着地する回答スプレッドシートは無償個人 Gmail アカウント所有のまま**である（2026-08-20 Drive API で実測）。ADR-0001 は「応募者の実データはフォーム本体ではなくこちらに蓄積される。移管対象はフォーム単体ではない」と明記しており、その指摘は未解消。

## Decision Outcome

選択: **案C-3**。

- 応募フォームは `osawa@yokohama-home.jp` 所有・**マイドライブ**に置く（共有ドライブは使わない）。
- 共有は `yokohama-home.jp` ドメイン全体に**閲覧者**、回答者は**リンクを知っている全員**。
- **回答先は既存の「採用」スプレッドシート**とする（2026-08-20 院長決定）。同シートは**無償個人 Gmail アカウント所有**で、かつ**リンクを知っている全員が閲覧可（`anyone` / `reader`）**であることを Drive API で実測したうえで、院長は**承知のうえでこの配置を選択**した。
- 個人 Gmail 所有のシート群は**いずれ `osawa@` 所有へ移す方針**（2026-08-20 院長。実施時期は未定）。
- **ADR-0001 の Decision Outcome のうち「@mdx-inc.co.jp の共有ドライブ」という置き場所の指定は本 ADR で supersede する。** ADR-0001 が示した「無償個人アカウント所有をやめる」という目的自体は維持する。

### Consequences

- Good: 履歴書アップロードを含め、旧フォームと**機能パリティを保ったまま**移行できた。
- Good: 設問投入は Forms API で完結でき、再作成・検証をコードで回せる。
- Bad: **共有ドライブではないため、所有者アカウントが失われると同種の事故が再発しうる**。ADR-0001 が共有ドライブを求めた理由（退職・アカウント削除への耐性）は解消していない。`osawa@` は代表者個人のアカウントであり role アカウントではない。
- Bad: 「MDX の応募者データを横浜ホームクリニックのテナントに置く」構成になった。法務論点は決着済み（上記）だが、**構成としてこうなっていること自体は記録に残す**。
- **Bad（最重要）: 応募者の実データは無償個人 Gmail アカウント所有・`anyone`/`reader` のスプレッドシートに着地する。** ADR-0001 が解こうとした「無償個人アカウント領域に応募者データが残り続ける」状態は**解消していない**。移行で変わったのはフォーム本体の所有だけである。
- Bad: `privacy.html` の「委託」表記の再判断（ADR-0001 実施順序6）は**本 ADR では変えていない**。
  2026-08-20 に一度「第三者提供」→「取扱いの委託」へ変更したが、**クロスレビュー指摘を受けてrevert した**。houmu-qa 2026-08-15 が委託成立の条件としたのは「新フォーム**と回答スプレッドシート**が Workspace 所有であること」であり、回答先が無償個人アカウント所有である以上、「委託」と公表する根拠がないため。**再判断の発火条件は「回答スプレッドシートが Workspace 所有になったとき」**であり、その時点で houmu-qa を 1 本取り直す。

## Confirmation

ADR-0001 のオラクル①「新フォームの `driveId` が非空（＝共有ドライブ配下）」は本決定により**成立しないため supersede**する。置き換え後のオラクル:

| # | 判定 | 実行方法 | 2026-08-20 の結果 |
|---|---|---|---|
| ① | 新フォームの所有者が **Workspace アカウント**であること（無償個人アカウントでない） | `GET drive/v3/files/<formId>?fields=owners` | `osawa@yokohama-home.jp` を確認・合格 |
| ② | 旧フォーム ID が repo 全体と本番ページから 0 件 | `git grep -c <旧formId>` / 本番 `careers.html` を curl して grep | repo・本番とも 0 件・合格 |
| ③ | 旧フォーム・旧スプレッドシートが削除済み（ゴミ箱も空） | `GET drive/v3/files/<id>` が 404 | **未実行**（削除前提ゲート (a)(b) が未成立のため） |
| ④ | 未同意で応募リンクが押せないこと | ブラウザで `#apply-consent` を切り替え `#apply-link` の `href` / `aria-disabled` を assertion | 合格（未同意=href なし+`true` → 同意で URL+`false` → 解除で復帰） |
| ⑤ | 新フォームに**履歴書アップロード設問が実在**すること（API で作れず UI 手作業のため一番落ちやすい） | `GET forms/v1/forms/<formId>` の items に `fileUploadQuestion` があること | 「履歴書・職務経歴書」を確認（最大5件・10MB）・合格 |
| ⑥ | **回答者が「リンクを知っている全員」**であること（組織限定のままだと外部応募者が 1 件も応募できず、静かに 0 件になる） | フォームの共有ダイアログで回答者ビューを確認。`publishSettings.publishState` も併せて確認 | 合格（既定は「横浜ホームクリニックの全ユーザー」だったため変更した） |
| ⑦ | **旧フォームが回答受付 OFF** であること（repo/本番の 0 件では外部求人媒体経由の流入を塞げない） | `GET forms/v1/forms/<旧formId>` の `publishState.isAcceptingResponses` が false（proto3 では省略）／回答者用 URL が `/closedform` へ遷移すること | 合格（両方で確認） |
| ⑧ | **テスト送信が回答先スプレッドシートの行として着地**すること（回答先の紐づけは API 非対応＝自動検証が存在しない唯一の箇所） | フォームから 1 件送信し「採用」シートに行が増えることを確認。確認後はシートの行と Forms 側の回答の**両方**を削除する | **未実施**（実データ投入を伴うため要 go） |

### 2026-08-20 に実施した本番操作（記録）

- 新フォームを作成し設問を投入（Forms API `batchUpdate`）。履歴書アップロード設問のみ UI で追加。
- 回答者を「リンクを知っている全員」へ変更（既定は組織限定だった）。
- 回答先を既存の「採用」スプレッドシートへ紐づけ（UI。API 非対応）。
- `careers.html` の応募リンクを新 URL へ差し替え、本番反映を確認。
- **旧フォームの回答受付を OFF**（Forms API `setPublishSettings`）。`/closedform` への遷移を機能確認。
  あわせて **旧フォームのタイトルへ誘導先を明記**した（`MDX株式会社 採用応募（受付終了：ご応募は mdx-inc.co.jp/careers.html へ）`）。`closedform` では説明文が表示されずタイトルしか出ないことを実測したため。
  ⚠ **旧フォーム本体・旧回答スプレッドシート・両者の連携は削除していない**（削除前提ゲート (a)(b) が未成立のため）。

なお **Forms API v1 は回答先スプレッドシートを設定できない**（Discovery Document 2026-08-16 版で `Form.linkedSheetId` が `readOnly: true` / "Output only"、`Request` に destination 系の操作が存在しないことを実測）。回答先の紐づけは UI かコンテナバインド Apps Script が必要で、この 1 点は自動化できない。
